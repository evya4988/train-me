// const mongoose = require('mongoose');

// const questionsSchema = new mongoose.Schema({
//     ReplyTo :  { type: Number, default: 'Public' }, //can ask or answer to any one
//     question: { type: String }, //we need to validate not allow empty form
//     answer: { type: String } //we need to validate not allow empty form
// });

// const Questions = mongoose.model("Questions", questionsSchema);


// module.exports = Questions;